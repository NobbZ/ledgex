mutation createUser(
  $first_name: String!
  $last_name: String!
  $email: String!
  $birthdate: Date
) {
  createUser(
    first_name: $first_name
    last_name: $last_name
    email: $email
    birthdate: $birthdate
  ) {
    id
    firstName
    lastName
    email
    birthdate
  }
}
